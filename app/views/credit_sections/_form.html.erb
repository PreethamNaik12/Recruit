<%= form_with(model: credit_section) do |form| %>
  <% if credit_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(credit_section.errors.count, "error") %> prohibited this credit_section from being saved:</h2>

      <ul>
        <% credit_section.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_area :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>