<%= form_with(model: credit_answer) do |form| %>
  <% if credit_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(credit_answer.errors.count, "error") %> prohibited this credit_answer from being saved:</h2>

      <ul>
        <% credit_answer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :answer %>
    <%= form.text_field :answer %>
  </div>

  <div class="field">
    <%= form.label :credit_question_id %>
    <%= form.text_field :credit_question_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
